---
title: "TME-Pisaura"
output: html_document
editor_options:
  chunk_output_type: console
---


## Packages

```{r packages}
require(ggplot2)
require(tidyr)
require(data.table)
require(dplyr)
```

## Harvest data

```{r data-harvest}
#read table
TME = fread("TME_Pisaura_data.csv", header = TRUE, encoding = 'Latin-1', stringsAsFactors = FALSE)

```

## Prepare data

```{r data-preparation}
#TODO maybe omit spiders that died during testing / NA values
TMEnoNA = na.omit(TME)
```

## Manipulate data

```{r data-manipulation}
#example for mean time on side for groups of different boldness levels
#cur_number() chooses intervals of equal number of observations
mn_time_side = TMEnoNA %>%
  mutate(boldness_groups = cut_number(boldness, n = 10)) %>%
  group_by(boldness_groups) %>%
  summarise(mean(t_on_B), mean(t_on_C))

#linear regression for time on side in relation to spider individuum
```
